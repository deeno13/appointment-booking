<%= link_to '< Back to appointments', trainer_appointments_path, class: 'block py-2 pl-3 pr-4 text-slate-500 font-medium hover:underline underline-offset-4 md:p-0' %>

<div class='mx-auto md:w-3/5 w-full flex mt-10'>
  <div class='mx-auto w-full'>
    <% if notice.present? %>
      <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
    <% end %>
    
    <%= render @appointment %>
</div>
